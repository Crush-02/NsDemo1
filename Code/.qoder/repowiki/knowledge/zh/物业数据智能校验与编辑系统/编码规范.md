- 校验结果统一使用 `ValidationResult` 接口定义，包含严重度（CRITICAL/HIGH/MEDIUM）及行列坐标信息。
- 样式更新采用批处理机制（batchSetCellFormat），通过 requestAnimationFrame 或 setTimeout 合并 DOM 操作以优化性能。
- 所有用户可见的错误消息在存入 Store 前必须经过 `sanitizeMessage` 进行术语标准化替换。
- 校验逻辑严格区分“即时格式校验”与“完整业务校验”，前者仅在输入时拦截明显格式错误，后者在失焦或导出时执行。